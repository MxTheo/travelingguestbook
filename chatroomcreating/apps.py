from django.apps import AppConfig

class ChatRoomcreatingConfig(AppConfig):
    """Configuration class for the ChatRoomcreating application."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'chatroomcreating'
